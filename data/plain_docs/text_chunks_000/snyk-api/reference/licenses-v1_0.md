This document uses the v1 API. For more details, see the v1 API.

When you import or update Projects, changes will be reflected in the endpoint results after a one-hour delay.
/licenses" method="post" %}
v1-api-spec.yaml
